<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="main w-full min-h-screen bg-zinc-900">
      <div class="form p-10">
        <form action="/create" method="post">
          <input
            type="text"
            placeholder="Title goes here"
            class="block px-5 py-2 bg-zinc-800 w-full rounded-lg outline-none text-white"
            name="title"
          />
          <textarea
            placeholder="Write your Task here"
            class="block px-5 py-2 resize-none bg-zinc-800 mt-2 w-full rounded-lg outline-none text-white"
            name="details"
          ></textarea>
          <input
            type="submit"
            value="Create Task"
            class="mt-2 bg-blue-600 px-5 py-2 rounded-md text-white cursor-pointer
            hover:bg-blue-700 transition-all duration-300"
          />
        </form>
      </div>
      <div class="tasks flex gap-3 flex-wrap p-10">
        <!-- equal to ejs block me tabhi likhte h jab print kraana but in case of logic -> if else , while loop , for lopp , we dont use "=" -->
        <% if(files.length>0){ %> <% files.forEach(function(val){ %>
        <div class="task min-w-72 px-3 py-4 bg-zinc-800 rounded-md">
          <h1 class="text-white text-3xl tracking-tighter"><%= val %></h1>

          <div class="flex w-full justify-between items-center mt-4">
            <a class="text-blue-600 inline-block" href="/file/<%= val %>"
              >Read More</a
            >
            <a class="text-zinc-600 hover:underline transition-all duration-200" href="/edit/<%= val %>">Edit Filename</a>
          </div>

        </div>

        <% }) %> <% }else{ %>
        <h1 class="text-zinc-600 text-xl tracking-tighter">No tasks found</h1>
        <% } %>

        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md hidden">
          <h1 class="text-white text-3xltracking-tighter">
            Some task goes here
          </h1>
          <a class="text-blue-600 inline-block mt-3" href="#">Read More</a>
        </div>

        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md hidden">
          <h1 class="text-white text-3xltracking-tighter">
            Some task goes here
          </h1>
          <a class="text-blue-600 inline-block mt-3" href="#">Read More</a>
        </div>

        <div class="task w-72 px-3 py-4 bg-zinc-800 rounded-md hidden">
          <h1 class="text-white text-3xl tracking-tighter">
            Some task goes here
          </h1>
          <a class="text-blue-600 inline-block mt-3" href="#">Read More</a>
        </div>
      </div>
    </div>
  </body>
</html>
